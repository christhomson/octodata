<% if event.ref_type == 'repository' %>
  <%= event.user.username %> created the <%= link_to_repository(event.repository) %> repository.
<% else %>
  <%= event.user.username %> created the <%= event.ref %> <%= event.ref_type %> on <%= link_to_repository(event.repository) %>.
<% end %>
